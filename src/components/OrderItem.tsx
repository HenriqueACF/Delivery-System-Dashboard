import {Box} from "@mui/material";
import {Order} from "@/types/Order";

type Props = {
    item: Order
}

export const OrderItem = ({item}: Props) => {
    return (
        <Box>
            ...
        </Box>
    )
}